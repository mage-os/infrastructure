name: MageOS Semantic Self-Checker
run-name: ${{ github.actor }} is self-checking the semantic version...
on: [pull_request]
jobs:
  call-semantic-version:
    uses: mage-os/infrastructure/.github/workflows/semantic-checker.yml@main

